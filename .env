# Define two completely separate image names to avoid Docker build cache conflicts.
SERVICE_IMAGE="${DOCKER_USERID}/house-price-predictor-service"
UI_IMAGE="${DOCKER_USERID}/house-price-predictor-ui"

# Define the tags/versions for each image
SERVICE_VERSION=v1.0.0
UI_VERSION=v1.0.0
